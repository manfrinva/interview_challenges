PT {
	Crie um algoritmo que:
	Crie um Array de inteiros de tamanho variado;
	Tenha numero gerado randomicamente;
	Retorne o menor e o maior valor entre os numeros do array;
	Use um metodo de busca com FOR;
	Use um metodo de busca sem usar FOR;
	Você deve exibir o tempo que a busca levou.
}

EN {
	Create an algorithm that:
	
Create an Array of integers of varying size;
	
Have randomly generated number;
	
Return the smallest and largest value between the numbers in the array;
	
Use a search method with FOR;
	
Use a search method without using FOR;

	You should display the time the search took.
}

ES {
	Cree un algoritmo que:

	Cree una matriz de números enteros de diferentes tamaños;
	
Tener un número generado aleatoriamente;

	Devuelve el valor más pequeño y más grande entre los números de la matriz;
	
Utilice un método de búsqueda con FOR;

	Utilice un método de búsqueda sin usar FOR;

	Debe mostrar el tiempo que tomó la búsqueda.
}